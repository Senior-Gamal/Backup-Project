@props(['title', 'count'])
<div class="bg-white dark:bg-gray-700 p-4 shadow rounded">
    <div class="text-gray-500 dark:text-gray-300">{{ $title }}</div>
    <div class="text-2xl font-bold">{{ $count }}</div>
</div>
